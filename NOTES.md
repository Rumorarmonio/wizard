# Рабочие заметки проекта

## Проект

Frontend-приложение onboarding wizard без бэкенда на Nuxt 4, Vue 3 и TypeScript.
Основной сценарий: пользователь проходит последовательные шаги формы, может двигаться вперёд и назад, сохраняет прогресс локально, проверяет введённые данные на шаге Review и завершает сценарий на экране Complete.

## Цель

Собрать минимальную рабочую версию многошагового мастера onboarding с шагами Account, Profile, Preferences, Review и Complete, с валидацией по шагам, сохранением данных в `localStorage` и аккуратным минималистичным интерфейсом без лишних библиотек.

## Текущее состояние

Репозиторий содержит только базовые проектные документы: `AGENTS.md`, `NOTES.md`, `PLAN.md`, `README.md` и `.gitignore`.
Код приложения, структура Nuxt-проекта и зависимости ещё не инициализированы.

## Ключевые решения

- Стек проекта: Nuxt 4 + Vue 3 + TypeScript.
- Стили: модульные SCSS-файлы, вынесенные в отдельные файлы рядом с компонентами или страницами.
- Архитектурный фокус: только frontend, без бэкенда и без внешней UI-библиотеки.
- Основной UX-поток: последовательные шаги с кнопками `Next` / `Back` и запретом перехода вперёд при невалидных данных текущего шага.
- Сохранение прогресса: черновик состояния wizard хранится в `localStorage`, чтобы пользователь мог продолжить после перезагрузки.
- Финальный поток: перед завершением пользователь видит шаг Review со сводкой всех введённых данных, после подтверждения попадает на экран Complete.
- Допущение: маршрутизация может быть либо внутри одной страницы с управлением текущим шагом, либо через вложенные компоненты без отдельного роутинга на каждый шаг; точный вариант будет выбран на этапе реализации.

## Что уже сделано

- Подготовлены базовые служебные документы для проекта.
- Зафиксированы продуктовые требования и рамки первой реализации до начала bootstrap.

## Текущие проблемы / открытые вопросы

- Нужно определить структуру хранения состояния wizard: единый composable/store или локальное состояние страницы с выделением утилит сохранения.
- Нужно выбрать стратегию валидации без лишних библиотек: нативные проверки и собственные функции либо лёгкая внутренняя схема валидации без внешнего пакета.
- Нужно определить минимальный набор полей для `Preferences`: список интересов, формат выбора уведомлений и дефолтные значения.

## Следующий шаг

Инициализировать Nuxt 4-проект и затем реализовать каркас wizard с базовой структурой шагов, общим состоянием и сохранением в `localStorage`.
